servers:
  - url: https://api.woosmap.com
tags:
  - Merchants API
description: >
  Retrieve clean names and logos in response to a dirty transactions POST.
security:
  - PrivateApiKeyAuth: []
requestBody:
  description: The request body must be formatted as JSON.
  required: true
  content:
    "application/json":
      schema:
        $ref: "../../schemas/MerchantsBrandRequest.yml"
      examples:
        default:
          value:
            $ref: "../../requests/woosmap_http_merchants_brand_request.yml"
responses:
  "200":
    description: 200 OK
    content:
      application/json:
        schema:
          $ref: "../../schemas/MerchantsBrandResponse.yml"
        examples:
          default:
            value:
              $ref: "../../responses/woosmap_http_merchants_brand_response.yml"
  "401":
    description: 401 UNAUTHORIZED
    content:
      application/json:
        examples:
          Unauthorized:
            value:
              $ref: "../../responses/woosmap_http_error_401_unauthorized_response.yml"