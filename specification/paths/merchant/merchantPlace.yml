servers:
  - url: https://api.woosmap.com
tags:
  - Merchant API
description: >
  Retrieve clean names, logos and Google Place ID of given dirty merchants name.
security:
  - PrivateApiKeyAuth: []
requestBody:
  description: The request body must be formatted as JSON.
  required: true
  content:
    "application/json":
      schema:
        $ref: "../../schemas/MerchantPlaceRequest.yml"
      examples:
        default:
          value:
            $ref: "../../requests/woosmap_http_merchant_brand_request.yml"
responses:
  "200":
    description: 200 OK
    content:
      application/json:
        schema:
          $ref: "../../schemas/MerchantPlaceResponse.yml"
        examples:
          default:
            value:
              $ref: "../../responses/woosmap_http_merchant_place_response.yml"
  "401":
    description: 401 UNAUTHORIZED
    content:
      application/json:
        examples:
          Unauthorized:
            value:
              $ref: "../../responses/woosmap_http_error_401_unauthorized_response.yml"