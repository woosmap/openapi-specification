name: limit
description: >
  If your request returns a high number of assets you should use the `limit` parameters to return only the first matching elements (Default is 5, max is 50).
schema:
  type: integer
  minimum: 1
  maximum: 50
example: 15
in: query