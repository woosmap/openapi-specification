title: SuccessZones
type: object
description: Message returned to a success request
properties:
  status:
    type: string
    description: the status of the 2OO response
    example: "success"
  message:
    description: message returned with the request
    example: "Zones successfully updated."
    type: string
